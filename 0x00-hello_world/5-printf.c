#include <stdio.h>
/**
 *main - prints a specified string
 *
 *Description: will print the message "with proper grammar, but
 *the outcome is a piece of art,"
 *Return: 0 if program runs without errors
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,");
	return (0);
}
